[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m pip install -r requirements-deploy.txt"]

[start]
cmd = "streamlit run dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
