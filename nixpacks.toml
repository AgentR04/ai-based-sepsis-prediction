[phases.setup]
nixPkgs = ["python311"]
cmds = ["python3 -m venv /opt/venv"]

[phases.install]
cmds = ["/opt/venv/bin/pip install -r requirements-deploy.txt"]

[start]
cmd = "streamlit run dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
